LAB 4 (Week 4): Sorting Algorithms
🟩 1️⃣ Insertion Sort
# Insertion Sort Implementation

def insertion_sort(arr):
    for i in range(1, len(arr)):
        key = arr[i]
        j = i - 1
        while j >= 0 and key < arr[j]:
            arr[j + 1] = arr[j]
            j -= 1
        arr[j + 1] = key


# --- Test the sorting ---
arr = [64, 34, 25, 5, 22]
print("Original array:", arr)
insertion_sort(arr)
print("Sorted array using Insertion Sort:", arr)


==================================================================================================
2️⃣ Merge Sort
# Merge Sort Implementation

def merge_sort(arr):
    if len(arr) > 1:
        mid = len(arr) // 2
        left = arr[:mid]
        right = arr[mid:]

        merge_sort(left)
        merge_sort(right)

        i = j = k = 0

        # Merge the sorted halves
        while i < len(left) and j < len(right):
            if left[i] < right[j]:
                arr[k] = left[i]
                i += 1
            else:
                arr[k] = right[j]
                j += 1
            k += 1

        # Copy remaining elements
        while i < len(left):
            arr[k] = left[i]
            i += 1
            k += 1

        while j < len(right):
            arr[k] = right[j]
            j += 1
            k += 1


# --- Test the sorting ---
arr = [64, 34, 25, 5, 22]
print("Original array:", arr)
merge_sort(arr)
print("Sorted array using Merge Sort:", arr)
=================================================================================================
3️⃣ Compare Performance using time Module
import random
import time

# Insertion Sort
def insertion_sort(arr):
    for i in range(1, len(arr)):
        key = arr[i]
        j = i - 1
        while j >= 0 and key < arr[j]:
            arr[j + 1] = arr[j]
            j -= 1
        arr[j + 1] = key

# Merge Sort
def merge_sort(arr):
    if len(arr) > 1:
        mid = len(arr) // 2
        left = arr[:mid]
        right = arr[mid:]
        merge_sort(left)
        merge_sort(right)
        i = j = k = 0
        while i < len(left) and j < len(right):
            if left[i] < right[j]:
                arr[k] = left[i]
                i += 1
            else:
                arr[k] = right[j]
                j += 1
            k += 1
        while i < len(left):
            arr[k] = left[i]
            i += 1
            k += 1
        while j < len(right):
            arr[k] = right[j]
            j += 1
            k += 1


# --- Compare performance ---
data = random.sample(range(1, 1000), 50)

# Insertion Sort Time
arr1 = data[:]
start = time.time()
insertion_sort(arr1)
end = time.time()
print("Insertion Sort Time:", end - start)

# Merge Sort Time
arr2 = data[:]
start = time.time()
merge_sort(arr2)
end = time.time()
print("Merge Sort Time:", end - start)
================================================================================================